import { Emoji } from '../../../constants';
import { SkillsEnum } from '../../types';

const Hunter = {
	aliases: ['hunter'],
	id: SkillsEnum.Hunter,
	emoji: Emoji.Hunter,
	name: 'Hunter'
};

export default Hunter;
